<%= form_for(subject_category) do |f| %>
  <% if subject_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subject_category.errors.count, "error") %> prohibited this subject_category from being saved:</h2>

      <ul>
      <% subject_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
