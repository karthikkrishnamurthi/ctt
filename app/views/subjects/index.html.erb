<p id="notice"><%= notice %></p>

<h1 align="center" style="color:blue;font-family: courier;"><u>Subjects</u></h1>

<table>
  <thead>
    <tr>
     <th>UniqueID</th>
      <th>Subject code</th>
      <th>Subject name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @subjects.each do |subject| %>
      <tr>
      <td><%= subject.id %></td>
        <td><%= subject.subject_code %></td>
        <td><%= subject.subject_name %></td>
      <!-- <td><%#= link_to 'Show', subject %></td>
        <td><%#= link_to 'Edit', edit_subject_path(subject) %></td>
        <td><%#= link_to 'Destroy', subject, method: :delete, data: { confirm: 'Are you sure?' } %></td>s</tr> -->
        <td><a href="/subjects/destroy/<%= subject.id %>">Delete</a></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Subject', new_subject_path %>
